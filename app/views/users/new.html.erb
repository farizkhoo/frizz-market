<div class="container">
	<h1>Sign up</h1>

	<div class="row">
		<div class="col-md-6">
			<%= form_for @user do |f| %>
				<%= render partial: '/users/form', object: f %>
				<%= f.submit "New User", class: "btn btn-primary" %>
			<% end %>
		</div>
	</div>
</div>